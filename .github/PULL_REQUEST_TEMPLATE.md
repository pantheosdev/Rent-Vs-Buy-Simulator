## Summary
Describe the change and why.

## Checklist
- [ ] `python run_all_qa.py` passes
- [ ] `python scripts/preflight.py --skip-qa` passes
- [ ] If UI changed: updated/verified visual snapshots (`tools/visual_regression/`)
- [ ] No breaking changes to `st.session_state` keys (or backward-compatible fallbacks added)

## Screenshots (if UI)
Attach before/after screenshots or baseline diffs.
